// utils.js file

const pi = 3.14;

export const add = (a, b) => a + b;

export const subtract = (a, b) => a - b;

export const breadPrices = [1, 2, 3, 4, 5, 6];

// option 2:
// const add = (a, b) => a + b;

// const subtract = (a, b) => a - b;

// const breadPrices = [1, 2, 3, 4, 5, 6];

// export { add, subtract, breadPrices };
